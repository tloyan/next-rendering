# Static Rendering

### 💡 Comprendre le rendu Statique

# Bienvenue

Ceci est un exemple de fichier **MDX** utilisant \`remark\` et \`remark-gfm\`.

## Tableau avec GFM

| Syntaxe   | Description |   Test Text |
| :-------- | :---------: | ----------: |
| Header    |    Title    | Here's this |
| Paragraph |    Text     |    And more |

## Liste de tâches

- [x] Ceci est une tâche terminée
- [ ] Ceci est une tâche non terminée

## Composant personnalisé

```tsx
import React from 'react'

interface IProps {
  title: string
}

const MyComponent: React.FC<IProps> = ({title}) => {
  return (
    <div>
      <h1>{title}</h1>
    </div>
  )
}

export default MyComponent
```

`;

## Utilisation de JSX et JavaScript

La date actuelle est : {new Date().getDate()}

\`\`\`js
console.log("Ceci est du code inline dans MDX");
\`\`\`
`;

# FlexBox

### **Partie 1 : Introduction à FlexBox**

Félicitation à toi 🎉

Tu viens de **maîtriser Css Grid** et honnêtement une fois que tu maitrise Css Grid, Flexbox va te sembler facile.

C’est un outil qui va te demander moins “d’organisation”.

Flexbox est un peu plus souple et moins structuré que Grid, ce qui le rend parfait pour les layouts qui nécessitent une flexibilité horizontale ou verticale. Il est idéal pour aligner des éléments dans une seule direction (horizontale ou verticale), ce qui en fait l'outil de choix pour les composants d'interface utilisateur comme les barres de navigation, les galeries, ou même pour des layouts entiers lorsqu'ils sont simples.

> Pour l’anecdote, lorsque j’ai appris les bases du html et css , grid était encore tout récent et donc j’utilisait flexbox pour tous mes layouts. Aujourd’hui il y a de nombreux cas dans lequel je suis content d’avoir Grid mais c’est pour te dire que FlexBox est puissant par sa simplicité et te permet de quasiment tout faire.

### **Petite Histoire de Flexbox**

Flexbox a été introduit pour la première fois dans les spécifications du W3C en 2009. Sa création visait à résoudre les problèmes que les développeurs rencontraient avec les anciennes méthodes de mise en page.

> Une minute de silence pour les développeurs qui devaient faire des hacks avec float et faire face aux problèmes d'alignement vertical qui donnaient des maux de tête atroce.

La simplicité et la flexibilité de Flexbox ont rapidement conquis la communauté des développeurs. Contrairement à CSS Grid qui est conçu pour gérer les deux dimensions (colonnes et lignes), Flexbox est unidimensionnel, se concentrant soit sur les colonnes, soit sur les lignes, ce qui le rend plus simple et direct pour certains types de layouts.

### **Pourquoi Flexbox ?**

Flexbox nous permet de créer des designs réactifs avec moins d'effort. La disposition des éléments devient intuitive, et les problèmes comme l'alignement vertical ou la création d'espaces égaux entre les éléments sont résolus élégamment. En maîtrisant Flexbox, tu auras un outil puissant dans ta boîte à outils de design web, capable de gérer un large éventail de situations avec simplicité et efficacité.

Dans les prochaines parties, nous plongerons dans les propriétés de Flexbox, apprendrons comment les utiliser pour créer des layouts élégants et explorerons des exemples concrets. Prépare-toi à ajouter une autre compétence essentielle à ton arsenal de développeur web !

## **Partie 2 : Pratique de FlexBox**

### 1/6 - Les propriétés

Tout comme dans le cours sur Css Grid, je t’ai préparé un tableau avec la liste des propriétés liées à Flex.

<aside>
⚠️ Fais un tour rapide des différentes propriétés et passe à la suite, on reviendra ici plus tard.

</aside>

| Propriété       | Valeurs                                                                      | Utilité                                                                                                |
| --------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| display         | flex ou inline-flex                                                          | Définit un élément comme un conteneur flex.                                                            |
| flex-direction  | row / row-reverse / column / column-reverse                                  | Définit la direction principale des éléments dans le conteneur flex.                                   |
| flex-wrap       | nowrap / wrap / wrap-reverse                                                 | Contrôle si les éléments sont forcés sur une seule ligne ou peuvent s'envelopper sur plusieurs lignes. |
| flex-flow       | row wrap / column nowrap                                                     | Raccourci pour flex-direction et flex-wrap.                                                            |
| justify-content | flex-start / flex-end / center / space-between / space-around / space-evenly | Alignement des éléments à l'intérieur du conteneur flex sur l'axe principal.                           |
| align-items     | stretch / flex-start / flex-end / center / baseline                          | Alignement des éléments sur l'axe transversal (perpendiculaire à l'axe principal).                     |
| align-content   | stretch / flex-start / flex-end / center / space-between / space-around      | Alignement des lignes flexibles dans l'espace multidimensionnel.                                       |
| order           | 0 / 1 / -1                                                                   | Contrôle l'ordre dans lequel les éléments apparaissent dans le conteneur flex.                         |
| flex-grow       | 0 / 1 / 2                                                                    | Définit la capacité d'un élément à croître pour remplir l'espace disponible.                           |
| flex-shrink     | 1 / 0                                                                        | Définit la capacité d'un élément à rétrécir si nécessaire.                                             |
| flex-basis      | auto / 100px / 50%                                                           | Définit la taille initiale d'un élément avant la répartition de l'espace restant.                      |
| flex            | 0 1 auto / 1 0 50%                                                           | Raccourci pour flex-grow, flex-shrink et flex-basis.                                                   |

### 2/6 - **Mise en Place de FlexBox**

Pour définir un élément Flex il suffit simplement de lui attribuer la propriété **`display`** avec la valeur **`flex`** ou **`inline-flex`**.

Si tu te demandes quelle est la différence entre les deux, voici grosso modo à quoi correspond chacun d’entre eux:

Pour comprendre **`inline-flex`** dis toi qu’il permet au conteneur flex de s'intégrer dans le flux en ligne (comme **`inline-block`** le ferait), tout en donnant à ses éléments internes un comportement flex (alignement, ordre, flexibilité).

Mais **99%** du temps nous utilisons **`display: flex`.** c’est très rare de voir `inline-flex` utilisé.

```html
<div class="flex_container">
  <div class="item_1">FLEX 1</div>
  <div class="item_2">FLEX 2</div>
  <div class="item_3">FLEX 3</div>
</div>
```

```css
.flex_container {
  display: flex;
}
```

Et voila , nous avons notre premier élément Flex !

Avec **`display: flex;`**, nos éléments **`div`**, qui sont des blocs par défaut (prenant toute la largeur disponible), se retrouvant alignés côte à côte sur une ligne. C'est le pouvoir de Flexbox : il transforme la manière dont les éléments enfants sont disposés, sans avoir besoin de propriétés supplémentaires.

### **3/6 - Comprendre le Flex Container**

Dans cette section, nous allons explorer en détail les propriétés du conteneur Flex et comment elles influencent les éléments enfants.

- **La propriété `flex-direction`**: montre comment cette propriété définit la direction principale des éléments flex.

![31selecPropriVal.jpg](/course/default.jpg)

![29selecPropriVal.jpg](FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/29selecPropriVal.jpg)

- **La propriété `flex-wrap`**: montre comment les éléments flex peuvent être forcés à rester sur une seule ligne ou à s'enrouler sur plusieurs lignes.

![30selecPropriVal (1).jpg](<FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/30selecPropriVal_(1).jpg>)

![33selecPropriVal.jpg](FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/33selecPropriVal.jpg)

- **La propriété `flex-flow`**: Introduis ce raccourci pour **`flex-direction`** et **`flex-wrap`**.
- **Les propriétés `justify-content`, `align-items`, et `align-content`**: montre comment aligner et répartir l'espace entre les éléments flex.

![32selecPropriVal (1).jpg](<FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/32selecPropriVal_(1).jpg>)

---

align-content et align-items sont souvent confondus, voici comment bien choisir entre les deux :

**`align-items`** : Imaginez que vous alignez des icônes sur une ligne de vos application téléphone. **`align-items`** détermine si les icônes sont alignées en haut de la ligne, centrées, ou alignées en bas.

**`align-content`** : Maintenant, pensez que vous avez plusieurs lignes d'icônes, comme votre écran d'accueil complet avec plusieurs lignes. Si votre écran n'est pas complètement rempli et que vous avez de l'espace supplémentaire en haut et en bas, **`align-content`** décide comment ces lignes d'icônes sont espacées verticalement sur l'écran entier.

En résumé :

- Utilisez **`align-items`** pour choisir comment placer les éléments individuellement dans la ligne (haut, milieu, bas).
- Utilisez **`align-content`** quand vous avez plusieurs lignes et de l'espace extra pour espacer ces lignes sur tout l'écran.

Si vous n'avez qu'une seule ligne d'icônes, **`align-content`** n’a pas d'effet.

### **4/6 - Maîtriser les Flex Items**

Cette section se concentre sur les éléments individuels à l'intérieur d'un conteneur Flex.

- **La propriété `order`**: Comment réarranger l'ordre visuel des éléments.
  ![34selecPropriVal.jpg](FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/34selecPropriVal.jpg)
- **La propriété `flex-grow`**: Enseignez comment un élément peut grandir pour remplir l'espace disponible.
- **La propriété `flex-shrink`**: Expliquez comment les éléments peuvent rétrécir pour s'adapter à l'espace disponible.
- **La propriété `flex-basis`**: Démontrez comment définir la taille initiale d'un élément flex.
- **La propriété `flex`**: Introduisez ce raccourci pour **`flex-grow`**, **`flex-shrink`**, et **`flex-basis`**.

Ces derniers points peuvent être souvent mal compris, alors nous allons essayer de voir ce qui les différence et à quoi chacun d’entre eux servent en simplifiant les propriétés **`flex-grow`**, **`flex-shrink`** et **`flex-basis`**. Ces propriétés déterminent comment un élément va s'adapter à l'espace disponible dans un conteneur Flex.

Imaginez que trois amis, Alice, Bob et Charlie, sont assis sur un banc qui peut s'étirer et se rétrécir. Ce banc est notre conteneur Flex.

![35selecPropriVal.jpg](FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/35selecPropriVal.jpg)

**`flex-grow`** : Alice aime l'espace. Si on lui donne un **`flex-grow`** de 1, elle s'étendra pour occuper tout l'espace libre sur le banc. Si Bob a un **`flex-grow`** de 2, il prendra deux fois plus d'espace libre que Alice. Charlie, avec un **`flex-grow`** de 0, ne s'étend pas du tout, il reste confortablement dans son espace initial.

![36selecPropriVal.jpg](FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/36selecPropriVal.jpg)

**`flex-shrink`** : Maintenant, supposons que le banc doit se rétrécir pour rentrer dans un espace plus petit. Si Alice a un **`flex-shrink`** de 1 et Charlie de 3, Charlie se rétrécira deux fois plus qu'Alice pour s'assurer qu'ils rentrent tous confortablement. Bob, avec un **`flex-shrink`** de 0, refuse de se rétrécir et garde sa taille, mettant la pression sur Alice et Charlie pour qu'ils se rétrécissent davantage.

![37selecPropriVal.jpg](FLEX%20BOX%200d9b4d9cddf34fd8bbfd368a73c3ce1a/37selecPropriVal.jpg)

**`flex-basis`** : C'est la quantité d'espace que chaque ami occupe au départ. Si on dit à Alice que son **`flex-basis`** est de 100px, elle commencera avec 100 pixels d'espace sur le banc avant de grandir ou de rétrécir. C'est l'espace personnel de départ d'Alice.

à noter que contrairement à width, flexbasis agit dans la direction du flex.

si `flex-direction: row` alors flex-basis agira sur la width.

si `flex-direction: column` alors flex-basis agira sur la height.

> Mon conseil si vous ne souhaitez pas avoir de surprise, c’est de travailler avec width et height plutôt que flex-basis, dans 99% des cas notre composant possède une taille et une forme bien particulière.

**`flex`** : C'est comme dire à nos amis comment se comporter avec un seul chiffre. Si on dit à Alice **`flex: 1 0 100px;`**, cela signifie qu'elle peut s'étendre (grandir), refuser de se rétrécir, et commencer avec 100 pixels d'espace.

En résumé, **`flex-grow`** est combien chacun s'étend, **`flex-shrink`** est combien chacun se rétrécit, et **`flex-basis`** est l'espace de départ de chacun. Cela aide Alice, Bob et Charlie à partager le banc de manière flexible, s'ajustant à sa taille changeante.

---

## Conclusion:

Ce chapitre sur flex est tout aussi important que celui sur les Grid mais il sera sûrement plus facilement assimilé.

À titre personnel , les propriétés flex sont celles que j’utilise le plus au quotidien.

C’est tellement pratique et facile que je ne m’en passe jamais.

## Exercice

suis la consigne de notre amis Mowgli 🐶

J’ai repris ce que nous avons construit précédemment dans Grid pour y intégrer des éléments Flex.

## Bonus 1

Ici je veux que tu t’amuses avec flex-shrink et flex-basis.

Ces deux propriétés sont surtout assimilées lorsque nous les manipulons.

Je vais donc te demander dans :

```css
.customBox {
  background: white;
  border-radius: 10px;
}

.customBox:nth-child(odd) {
}
```

D’ajouter dans un premier temps flex-basis de 100px à notre customBox.

Puis ajoute les propriétés flex-grow et flex-shrink aux 2 customBox.

J’espère que tu n’as pas oublié à quoi servait ceci : `:nth-child(odd)` 🫠

voici un petit rafraichissement de mémoire : [documentation](https://developer.mozilla.org/fr/docs/Web/CSS/:nth-child)

## Allez plus loin

- FlexBox

📑 [Le lien vers la documentation](https://developer.mozilla.org/fr/docs/Web/CSS/CSS_flexible_box_layout/Basic_concepts_of_flexbox)

- nth-child

📑 [Le lien vers la documentation](https://developer.mozilla.org/fr/docs/Web/CSS/:nth-child)

## 🐜 Feedback

Remplir [le formulaire de feedback](https://go.mikecodeur.com/cours-react-avis?entry.1430994900=React%20Pr%C3%A9requis%20d%C3%A9butants&entry.533578441=1%20Rappel%20HTML%20CSS).

```jsx
https://go.mikecodeur.com/cours-react-avis?entry.1912869708=TypeScript%20PRO&entry.1430994900=2.Les%20Fondamentaux&entry.533578441=01%20Les%20bases
```
